# Routes

React Router is configured in `src/App.jsx`.

- `/shop` — Shop content and reference product cards
- `/merch` — Merch listing using `CatalogProductCard`
- Product detail routes — product detail pages
