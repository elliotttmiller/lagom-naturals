# Shared layout

The React application uses `src/App.jsx` as the route shell with shared navigation and footer surfaces. Product-card work is self-contained in `CatalogProductCard` and does not change the global shell.
